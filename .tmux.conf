# Set prefix to Ctrl-a
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# Enable mouse support
set -g mouse on

# Set base index to 1
set -g base-index 1

# Set pane base index to 1
setw -g pane-base-index 1

# Enable activity alerts
setw -g monitor-activity on
set -g visual-activity on

# Set vi mode for copy mode
setw -g mode-keys vi

# Modern colors
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",xterm-256color:Tc"

# Vim style pane selection
bind h select-pane -L
bind j select-pane -D 
bind k select-pane -U
bind l select-pane -R

# Use Alt-arrow keys without prefix key to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Increase scrollback buffer size
set -g history-limit 10000

# Shift arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# Reload tmux config
bind r source-file ~/.tmux.conf \; display-message "tmux.conf reloaded."

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'tmux-plugins/tmux-open'
set -g @plugin 'tmux-plugins/tmux-online-status'
set -g @plugin 'tmux-plugins/tmux-net-speed'

# Status bar customization
set -g status on
set -g status-interval 1
set -g status-justify centre
set -g status-position bottom
set -g status-left-length 90
set -g status-right-length 90
set -g status-style "bg=#1e1e2e"

# CPU and RAM format
set -g @cpu_percentage_format "%3.0f%%"
set -g @ram_percentage_format "%3.0f%%"

# Status bar content
set -g status-left "#[fg=#89b4fa,bg=#1e1e2e] 󱫋 #S "
set -g status-right "#[fg=#89b4fa]󰻠 CPU:#{cpu_percentage} 󰍛 RAM:#{ram_percentage} #{battery_status_fg}#{battery_icon} #{battery_percentage} #{net_speed} #[fg=#cdd6f4]%a %d %b %H:%M:%S "

# Window style
setw -g window-style 'bg=#1e1e2e'
setw -g window-active-style 'bg=#313244'
setw -g window-status-format '#[fg=#cdd6f4,bg=#1e1e2e] #I #[fg=#cdd6f4,bg=#1e1e2e] #W '
setw -g window-status-current-format '#[fg=#1e1e2e,bg=#89b4fa] #I #[fg=#1e1e2e,bg=#89b4fa] #W '

# Pane border style
set -g pane-border-style 'fg=#313244'
set -g pane-active-border-style 'fg=#89b4fa'

# Message style
set -g message-style 'fg=#cdd6f4 bg=#1e1e2e'

# Command prompt style
set -g message-command-style 'fg=#cdd6f4 bg=#1e1e2e'

# Mode style
setw -g mode-style 'fg=#cdd6f4 bg=#313244'

# Battery icons and settings
set -g @batt_icon_status_charged "󰁹"
set -g @batt_icon_status_charging "󰂄"
set -g @batt_icon_status_discharging "󰁾"
set -g @batt_icon_status_attached "󰚥"
set -g @batt_icon_status_unknown "󰂑"

set -g @battery_icon "🔋"
set -g @battery_icon_charging "⚡"
set -g @battery_icon_attached "🔌"
set -g @battery_status_fg "#[fg=#89b4fa]"
set -g @battery_status_charging_fg "#[fg=#a6e3a1]"
set -g @battery_status_attached_fg "#[fg=#f9e2af]"
set -g @battery_status_discharging_fg "#[fg=#f38ba8]"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

# Theme settings
set -g @catppuccin_flavour 'mocha'
set -g @catppuccin_window_left_separator ""
set -g @catppuccin_window_right_separator ""
set -g @catppuccin_window_middle_separator " █"
set -g @catppuccin_window_number_position "right"
set -g @catppuccin_window_default_fill "none"
set -g @catppuccin_window_current_fill "all"

# Disable default modules to use custom ones
set -g @catppuccin_status_modules_right ""
set -g @catppuccin_status_modules_left ""
set -g @catppuccin_status_left_separator  ""
set -g @catppuccin_status_right_separator ""
set -g @catppuccin_status_right_separator_inverse "no"
set -g @catppuccin_status_fill "none"
set -g @catppuccin_status_connect_separator "no"

# CPU and RAM settings
set -g @sysstat_cpu_view_tmpl '#[fg=#89b4fa]CPU:#[default] #{cpu.pused}'
set -g @sysstat_mem_view_tmpl '#[fg=#89b4fa]RAM:#[default] #{mem.pused}'
set -g @sysstat_swap_view_tmpl '#[fg=#89b4fa]SWAP:#[default] #{swap.pused}'
set -g @sysstat_cpu_low_fg_color "#[fg=#a6e3a1]"
set -g @sysstat_cpu_medium_fg_color "#[fg=#f9e2af]"
set -g @sysstat_cpu_high_fg_color "#[fg=#f38ba8]"
set -g @sysstat_mem_low_fg_color "#[fg=#a6e3a1]"
set -g @sysstat_mem_medium_fg_color "#[fg=#f9e2af]"
set -g @sysstat_mem_high_fg_color "#[fg=#f38ba8]"

# Battery settings
set -g @battery_icon "🔋"
set -g @battery_icon_charging "⚡"
set -g @battery_icon_attached "🔌"
set -g @battery_status_fg "#[fg=#89b4fa]"
set -g @battery_status_charging_fg "#[fg=#a6e3a1]"
set -g @battery_status_attached_fg "#[fg=#f9e2af]"
set -g @battery_status_discharging_fg "#[fg=#f38ba8]" 
